<p id="notice"><%= notice %></p>

<h1>Actormovies</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @actormovies.each do |actormovie| %>
      <tr>
        <td><%= link_to 'Show', actormovie %></td>
        <td><%= link_to 'Edit', edit_actormovie_path(actormovie) %></td>
        <td><%= link_to 'Destroy', actormovie, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Actormovie', new_actormovie_path %>
