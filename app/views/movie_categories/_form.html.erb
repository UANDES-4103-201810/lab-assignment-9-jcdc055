<%= form_with(model: movie_category, local: true) do |form| %>
  <% if movie_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_category.errors.count, "error") %> prohibited this movie_category from being saved:</h2>

      <ul>
      <% movie_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
